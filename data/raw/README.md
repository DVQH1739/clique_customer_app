# Raw data

Place the Online Retail II workbook here:

```
online_retail_ii.xlsx
```

Download from the UCI / retailer source used in your course, then copy into this folder.
The pipeline reads `data/raw/online_retail_ii.xlsx` by default (`config.ONLINE_RETAIL_XLSX`).

Excel files are gitignored (large); only this README is version-controlled.
